{
    "title": "iad Spain Scraper Input",
    "type": "object",
    "schemaVersion": 1,
    "properties": {
        "maxProvinces": {
            "title": "Nombre max de provinces",
            "type": "integer",
            "description": "Limite le nombre de provinces à scraper (52 au total). Laisser vide pour tout scraper.",
            "nullable": true,
            "editor": "number"
        },
        "supabaseUrl": {
            "title": "Supabase URL (staging)",
            "type": "string",
            "description": "URL de votre projet Supabase staging (ex: https://xxxxx.supabase.co)",
            "editor": "textfield"
        },
        "supabaseKey": {
            "title": "Supabase Service Key (staging)",
            "type": "string",
            "description": "Clé API Supabase avec permissions d'écriture (service_role)",
            "editor": "textfield",
            "isSecret": true
        }
    },
    "required": []
}
